<?xml version="1.0" encoding="utf-8"?>
<local:ModuleView
  x:Class="TermBar.Views.SettingsView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:local="using:TermBar.Views"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d">
  <Grid
    x:Name="Settings">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
    </Grid.ColumnDefinitions>
    <StackPanel
      Orientation="Vertical"
      Grid.Row="0"
      Grid.Column="0">
      <TextBlock
        Style="{StaticResource Prose}"
        Text="The TermBar configuration file is located here (click it to copy it):"/>
      <TextBlock
        x:Name="ConfigPathTextBlock"
        PointerPressed="ConfigPathTextBlock_PointerPressed"
        Style="{StaticResource Prose}"
        Text="{x:Bind ConfigPath}"/>
      <TextBlock
        Style="{StaticResource Prose}"
        Text="Edit it in your favorite editor and restart TermBar to see the changes."/>
    </StackPanel>
    <StackPanel
      x:Name="Buttons"
      Grid.Row="1"
      Grid.Column="0">
      <HyperlinkButton
        x:Name="ShowInExplorer"
        Click="ShowInExplorer_Click">
        <TextBlock Text="Show Config File in Explorer"/>
      </HyperlinkButton>
      <Button
        x:Name="OK"
        Click="OK_Click">
        <TextBlock Text="OK"/>
      </Button>
      <Button
        x:Name="Exit"
        Style="{StaticResource AccentedButton}"
        Click="Exit_Click">
        <TextBlock
          Style="{StaticResource AccentedTextBlock}"
          Text="Exit TermBar"/>
      </Button>
      <Button
        x:Name="Restart"
        Click="Restart_Click">
        <TextBlock Text="Restart TermBar"/>
      </Button>
    </StackPanel>
  </Grid>
</local:ModuleView>
